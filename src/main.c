// calculation of compound interest
#include <stdio.h>
int main() {
//declaration and intiaisation
double principal = 20000;
double rate =4.5 // 4.5% interest rate
double time = 4; // 4 years

double amount = principal*((pow((1+rate/n),n*time)));
printf("the amount(double) is %f",amount);
return 0;
}
	
